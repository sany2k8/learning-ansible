# Module 8 — Handlers & Notifications

## Concepts Covered
- **notify**: Used to trigger a handler when a task makes changes (updates state).
- **handlers**: Special tasks that run only when notified, at the end of the play.
- **Idempotency optimization**: Restarts service only if configuration templates/files changed.
